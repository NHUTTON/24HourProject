import React, {useState} from 'react';

const Weather = (props) => {
    console.log(props.latitude, props.longitude);

    return(
        <div>
            <h1>Hello</h1>
        </div>
    )
}

export default Weather;